version: 2

models:
  - name: stg_events
    description: "Cleaned staging layer for raw events."
    columns:
      - name: event_id
        description: "Unique identifier for the event."
        tests:
          - not_null
  - name: stg_users
    description: "Cleaned staging layer for raw users."
    columns:
      - name: user_id
        description: "Unique identifier for the user."
        tests:
          - not_null
  - name: dim_user
    description: "Gold dimension table at user grain."
    columns:
      - name: user_id
        description: "Business key maintained for lookups."
        tests:
          - unique
          - not_null
  - name: fact_events
    description: "Gold fact table at event grain."
    columns:
      - name: event_id
        description: "Business key for the fact record."
        tests:
          - unique
          - not_null

# TODO: Add a custom data test to assert allowed event_type values.
